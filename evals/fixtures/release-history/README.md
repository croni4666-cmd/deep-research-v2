# Release-history fixture

Candidate runners may inspect only `sources/`. The files deliberately mix a
current stable release with a development build, a prerelease, an older stable
release, and a yanked release. `ground-truth.json` is evaluator-only.
