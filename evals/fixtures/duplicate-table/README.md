# Duplicate-table fixture

Candidate runners may inspect only `sources/` and must produce one concise
Markdown comparison table containing each benchmark exactly once.
`ground-truth.json` is evaluator-only.
