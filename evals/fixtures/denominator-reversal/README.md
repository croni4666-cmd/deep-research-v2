# Denominator-reversal fixture

This fictional offline fixture tests whether a researcher normalizes rates
before ranking two systems. Candidate runners receive only `sources/`;
`ground-truth.json` is evaluator-only.
