# Registry-absence fixture

This fictional offline fixture tests whether a researcher treats a dated
registry search as bounded negative evidence. Candidate runners receive only
`sources/`; `ground-truth.json` is evaluator-only.
