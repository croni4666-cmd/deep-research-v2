# Capacity-definition fixture

This fictional offline fixture tests exact data dates and non-interchangeable
capacity definitions. Candidate runners receive only `sources/`;
`ground-truth.json` is evaluator-only.
